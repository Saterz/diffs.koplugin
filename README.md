# diffs.koplugin

A KOReader plugin for viewing the changes between two revisions of a public GitHub repository.

This plugin was fully generated by artificial intelligence. Human review, testing, and project direction remain important, but the source code was produced by AI. See [CONTRIBUTING.md](docs/CONTRIBUTING.md) for development and contribution guidance.

## Installation

The recommended installation method is [ZenPM](https://github.com/omerjerk/zenpm) using the Saterz plugin repository:

```text
https://zenpm-repo.saterz.dev
```

Add the repository to ZenPM, then install `diffs.koplugin` through ZenPM.

Manual installation is also possible. Download the latest archive from the [GitHub Releases page](https://github.com/Saterz/diffs.koplugin/releases), then extract its `diffs.koplugin` directory into KOReader's plugin directory:

```text
koreader/plugins/diffs.koplugin/
```

Restart KOReader, open **Tools → Diffs**, and enter:

1. A repository owner such as `koreader`.
2. A repository name such as `koreader`.
3. A base commit, branch, or tag.
4. A head commit, branch, or tag.

The plugin uses GitHub's unauthenticated API and therefore supports public repositories only. GitHub may temporarily reject requests after its anonymous rate limit is reached.

## Features

- Compare commit SHAs, branches, or release tags through GitHub's compare API.
- Parse unified diffs into files, hunks, and numbered lines.
- Display combined or split layouts.
- Toggle line wrapping and remember viewer preferences.
- Emphasize paired character changes within modified lines.
- Preserve unknown Git file headers and report malformed hunk counts as warnings.
- Scrub quickly through diffs with a touch-friendly e-ink scrollbar.
- Identify additions and removals without relying on color.

## Viewer controls

- **×** closes the viewer.
- **⚙** opens persistent settings for line wrapping and layout.
- Swipe north or south to move by a page.
- With wrapping disabled, swipe west or east to scroll long lines horizontally.
- Drag the scrollbar to scrub rapidly or tap its rail to jump without dragging.

## Releases

Releases are created automatically by GitHub Actions when a version tag is pushed. Tags use the form `vMAJOR.MINOR.PATCH`, optionally followed by a prerelease suffix such as `-beta.1` or `-rc.1`.

The workflow checks the codebase, packages the Lua files, and attaches `diffs.koplugin.zip` to the GitHub Release. Beta tags must point to a commit reachable from the `dev` branch.

## Development

The pure-Lua comparison model, diff parser, line pairing, intraline logic, and scrollbar calculations have specifications in `spec/`.

See [CONTRIBUTING.md](docs/CONTRIBUTING.md) for development setup, testing, packaging, and pull request guidelines.

## Known limitations

- Git combined-merge diffs are not supported.
- Git binary patch payloads are identified but not decoded.
- Syntax highlighting and user-configurable themes are not currently supported.

## License

This project is released under [The Unlicense](LICENSE). Because the plugin was fully generated by AI, it is intentionally distributed without restrictive copyright conditions and, to the extent legally possible, dedicated to the public domain.
