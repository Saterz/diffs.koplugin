# diffs.koplugin

A KOReader plugin for viewing the changes between two GitHub revisions.

The MVP accepts a public GitHub repository URL plus base and head Git references,
downloads the comparison as a unified diff, and opens an e-ink-oriented viewer.

